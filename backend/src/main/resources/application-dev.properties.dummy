# You can create the file `application-dev.properties` and run Spring with that settings by adding `-Dspring.profiles.active=test` when you start Spring
# See https://blog.indrek.io/articles/externalized-configuration-in-spring-boot/

spring.datasource.url = jdbc:mysql://${MYSQL_HOST:localhost}:3306/kickercup
spring.datasource.username = kickercup2-user
spring.datasource.password = admin